# input_ids
input_ids 是一段文本经过分词器（Tokenizer）处理后，转换成的“整数列表”或“整数张量（Tensor）”。
维度为[batch_size, seq_len, 1]

# logits

指的是经过attention计算完之后，每前n个token对下一个token预测的概率，
logits的维度为[Batch_size, Seq_len, vab_size]